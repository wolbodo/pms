


frontend: 
    build: frontend
    ports:
        - "80"
    environment:
        - VIRTUAL_HOST=pms.dxlb.nl

backend:
    build: backend
    ports: 
        - "4242"
    environment: 
        - VIRTUAL_HOST=pmsapi.dxlb
        - VIRTUAL_PORT=4242
